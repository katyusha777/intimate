// Drizzle schema — intentionally empty. The database schema is planned
// together in a dedicated session (see GitHub Issues), not scaffolded.
// All enums/CHECK values must come from src/lib/taxonomy.ts.

export {};
