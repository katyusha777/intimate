---
/** App-facing Skeleton — wraps ui/skeleton (vendor). See atoms/Button.astro. */
import { Skeleton as UiSkeleton } from '@/components/ui/skeleton';
---

<UiSkeleton {...Astro.props}><slot /></UiSkeleton>
